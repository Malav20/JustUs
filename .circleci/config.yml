version: 2.1

executors:
  android-executor:
    docker:
      - image: cimg/android:2024.11-node
    environment:
      JVM_OPTS: -Xmx3200m
      NEXT_PUBLIC_SUPABASE_URL: "https://djuqnhqedykhectfhzba.supabase.co"
      NEXT_PUBLIC_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRqdXFuaHFlZHlraGVjdGZoemJhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc2MzMxNzcsImV4cCI6MjEwMzIwOTE3N30.UhcqK9MfjxuT-XjjiHzpnrRgZKjkyX2IDuh5FMhoO98"
      SUPABASE_SERVICE_ROLE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRqdXFuaHFlZHlraGVjdGZoemJhIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NzYzMzE3NywiZXhwIjoyMTAzMjA5MTc3fQ.-3pFyBLu28GY1zlWd2yNQp2V4s8qH3ia-BQ_9HwePJk"
      SUPABASE_URL: "https://djuqnhqedykhectfhzba.supabase.co"
      NEXT_PUBLIC_LIVEKIT_URL: "wss://justus-0q7zbww8.livekit.cloud"
      LIVEKIT_API_KEY: "APIukxmynV6MQkR"
      LIVEKIT_API_SECRET: "OiAeIxN1foN0UQTbvdWW4veSRC4rtTNZua64vC9Qzl3A"
      NEXT_PUBLIC_APP_URL: "https://just-us-web.vercel.app"

  ios-executor:
    macos:
      xcode: "15.4.0"
    environment:
      NEXT_PUBLIC_SUPABASE_URL: "https://djuqnhqedykhectfhzba.supabase.co"
      NEXT_PUBLIC_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRqdXFuaHFlZHlraGVjdGZoemJhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODc2MzMxNzcsImV4cCI6MjEwMzIwOTE3N30.UhcqK9MfjxuT-XjjiHzpnrRgZKjkyX2IDuh5FMhoO98"
      SUPABASE_SERVICE_ROLE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRqdXFuaHFlZHlraGVjdGZoemJhIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NzYzMzE3NywiZXhwIjoyMTAzMjA5MTc3fQ.-3pFyBLu28GY1zlWd2yNQp2V4s8qH3ia-BQ_9HwePJk"
      SUPABASE_URL: "https://djuqnhqedykhectfhzba.supabase.co"
      NEXT_PUBLIC_LIVEKIT_URL: "wss://justus-0q7zbww8.livekit.cloud"
      LIVEKIT_API_KEY: "APIukxmynV6MQkR"
      LIVEKIT_API_SECRET: "OiAeIxN1foN0UQTbvdWW4veSRC4rtTNZua64vC9Qzl3A"
      NEXT_PUBLIC_APP_URL: "https://just-us-web.vercel.app"

jobs:
  build-android-apk:
    executor: android-executor
    steps:
      - checkout
      - run:
          name: Print Tool Versions
          command: |
            node -v || true
            npm -v || true
            java -version || true
      - run:
          name: Install Dependencies
          command: |
            npm --prefix apps/web install
      - run:
          name: Build Next.js App & Sync Android
          command: |
            cd apps/web
            mkdir -p out android/app/src/main/assets
            npm run build
            npx --yes @capacitor/cli sync android
      - run:
          name: Build Debug APK with Gradle
          command: |
            cd apps/web/android
            chmod +x gradlew
            ./gradlew assembleDebug
      - run:
          name: Prepare APK Output
          command: |
            cp apps/web/android/app/build/outputs/apk/debug/app-debug.apk JustUS.apk
      - run:
          name: Upload APK to Supabase Storage
          command: |
            node scripts/upload-to-supabase.mjs JustUS.apk JustUS.apk
      - store_artifacts:
          path: JustUS.apk
          destination: JustUS.apk
      - run:
          name: Release APK to GitHub Releases
          command: |
            if [ -n "$GITHUB_TOKEN" ] && { [ "$CIRCLE_BRANCH" = "main" ] || [ "$CIRCLE_BRANCH" = "master" ]; }; then
              echo "Publishing APK to GitHub Release 'latest'..."
              gh release upload latest JustUS.apk --clobber || echo "GitHub release upload skipped or completed with status $?"
            else
              echo "Skipping GitHub release: GITHUB_TOKEN not configured in CircleCI or not on main/master branch."
            fi

  build-ios-ipa:
    executor: ios-executor
    steps:
      - checkout
      - run:
          name: Check / Install Node.js
          command: |
            if ! command -v node &> /dev/null; then
              brew install node
            fi
            node -v
            npm -v
      - run:
          name: Install Dependencies
          command: |
            npm --prefix apps/web install
      - run:
          name: Build Next.js App & Sync iOS
          command: |
            cd apps/web
            mkdir -p out ios/App/App/public
            npm run build
            npx --yes @capacitor/cli sync ios
      - run:
          name: Build Unsigned iOS IPA
          command: |
            cd apps/web/ios/App
            xcodebuild -project App.xcodeproj -scheme App -configuration Release -destination 'generic/platform=iOS' -archivePath App.xcarchive archive CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
            mkdir -p Payload
            cp -r App.xcarchive/Products/Applications/App.app Payload/
            zip -r JustUS.ipa Payload/
      - run:
          name: Upload IPA to Supabase Storage
          command: |
            node scripts/upload-to-supabase.mjs apps/web/ios/App/JustUS.ipa JustUS.ipa
      - store_artifacts:
          path: apps/web/ios/App/JustUS.ipa
          destination: JustUS.ipa
      - run:
          name: Release IPA to GitHub Releases
          command: |
            if [ -n "$GITHUB_TOKEN" ] && { [ "$CIRCLE_BRANCH" = "main" ] || [ "$CIRCLE_BRANCH" = "master" ]; }; then
              echo "Publishing IPA to GitHub Release 'latest'..."
              gh release upload latest apps/web/ios/App/JustUS.ipa --clobber || echo "GitHub release upload skipped or completed with status $?"
            else
              echo "Skipping GitHub release: GITHUB_TOKEN not configured in CircleCI or not on main/master branch."
            fi

workflows:
  build-apps:
    jobs:
      - build-android-apk:
          filters:
            branches:
              only:
                - main
                - master
      - build-ios-ipa:
          filters:
            branches:
              only:
                - main
                - master